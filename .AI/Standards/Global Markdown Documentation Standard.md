# Global Markdown Standard

One H1; unique headings; GFM; `-` bullets only; pipe tables; fenced code; literal paths in code; blank line before horizontal rules; preserve filename; full replacement; validate links and no `*` bullets.
