# Run Control

Project identity precedes queue access. Resolver owns task promotion, claims, completion, and advancement. Ad hoc work is valid but must pass project validation before Codex execution.
