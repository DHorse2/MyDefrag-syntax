# Output Formatting

Use the requested format; otherwise Markdown. Keep structure readable, paths literal, tables where clearer, and artifacts downloadable. Follow `Global Markdown Documentation Standard.md`.
