# AI Instructions

```yaml
preserve: [comments, public APIs, unrelated formatting, existing behavior]
change: requested-only
prefer: [additive changes, minimal diff, root-cause diagnosis, deterministic evidence]
prohibitWithoutApproval: [git commit, git push, ovsx publish, vsce publish, npm publish]
multifile: PLAN_AND_LIST_FIRST
completion: [summary, files, validation, risks, next]
record: Standards\Execution_Record_Standard.md
markdown: Standards\Global Markdown Documentation Standard.md
```
